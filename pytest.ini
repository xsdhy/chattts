[pytest]
# 后端自动化测试配置。
# - asyncio_mode=auto：异步测试函数无需逐个标注 @pytest.mark.asyncio。
# - testpaths：只收集项目自身的 tests 目录，避免扫描 .venv 里第三方库的测试。
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
